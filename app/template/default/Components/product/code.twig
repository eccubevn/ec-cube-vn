{% if product.code_min is not empty %}
  <div class="product-code {{class|default('mb-2')}}">
    {{ 'front.product.code'|trans }}:  
    <strong class="product-code-default">
      {{ product.code_min }}{% if product.code_min != product.code_max %} ～ {{ product.code_max }}{% endif %}
    </strong>
  </div>
{% endif %}