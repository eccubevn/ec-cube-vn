{% extends 'Components/form/base_input.twig' %}
{% set custom_class = customClass|default('') %}

{% block formLabel %}
    {{ form_label(form.sex, 'common.gender', { 'label_attr': { 'class': 'col-form-label' }}) }}
{% endblock %}

{% block formContent %}
    <div class="row {{ has_errors(form.sex) ? 'is-invalid'}}">
        <div class="col-12">
            {{ form_widget(form.sex, { 'attr': { 'class': 'form-control' }}) }}
            {{ form_errors(form.sex) }}
        </div>
    </div>
{% endblock %}